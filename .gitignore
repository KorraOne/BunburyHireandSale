# --- Secrets / environment ---
.env
.env.*
!.env.example

# --- Node / npm ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Runtime files ---
*.log

# --- OS / editor ---
.DS_Store
Thumbs.db
Desktop.ini

# VS Code / Cursor
.vscode/
.cursor/

# --- Build / cache ---
dist/
build/
.cache/

# --- Data ---
# Keep your items list in git (required for the site to work),
# but ignore local-only data dumps if you add them later.
data/*.bak
data/*.tmp