# --- Secrets / environment ---
.env
.env.*
!.env.example

# --- Node / npm ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
node_trace.*.log

# --- Runtime files ---
*.log

# --- OS / editor ---
.DS_Store
Thumbs.db
Desktop.ini

# VS Code / Cursor
.vscode/
.cursor/

# --- Build / cache ---
# dist/ is deploy output; keep it tracked if desired.
build/
.cache/

# dist/data is runtime/admin-managed content; never track it.
dist/data/

# Uploaded product images live in dist/ at runtime; never track them.
dist/public/images/products/

# Uploads no longer go to repo root public/; keep it clean.
public/images/products/

# --- Local-only data / backups ---
data/*.bak
data/*.tmp
data/contact_submissions.json

# --- Optional local folders (keep repo clean) ---
coverage/

data/items.json
public/images/products/
